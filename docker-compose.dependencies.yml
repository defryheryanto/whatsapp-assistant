version: "3.4"
services:
  db:
      image: postgres:latest
      container_name: whatsapp_assistant_db
      environment:
        - POSTGRES_DB=whatsapp_assistant_db
        - POSTGRES_PASSWORD=password
      ports:
        - "5435:5432"

